
> En la mañana moví una estufa y encontré dos pesos. Si quieres te los regalo.

Recordemos que todo lo que va después de las dos líneas, se ejecuta en todos los procesadores.

```
// Las líneas comentadas solo estána así para que no salgan errores. Descomentar para ejecución.

#include<stdio.h>
#include<string.h>
//#include<mpi.h>

int main(int argc, char **argv)
{
	int identificador,cuenta;
	char mensaje[50];
	//MPI_Status estatus;
	//MPI_Init(&argc, &argv);
	//MPI_Comm_rank(MPI_COMM_WORLD,&identificador);
	
	if (identificador==0) 
	{
		printf("Nodo maestro. Mandando el mensaje.\n\n");
		strcpy(mensaje,"Hola mundo desde el nodo maestro\n\n");
        for(int i=1; i<10;i++){
		    MPI_Send(mensaje,sizeof(mensaje),MPI_CHAR,1,100,MPI_COMM_WORLD);
        }
	}
	else 
	{
		printf("Nodo esclavo %d. Recibiendo el mensaje.\n",identificador);
		//MPI_Recv(mensaje,sizeof(mensaje),MPI_CHAR,0,100,MPI_COMM_WORLD,&estatus);
		printf ("El mensaje es: %s\n", mensaje);
		//MPI_Get_count(&estatus,MPI_CHAR,&cuenta);
		//printf("Recibidos %d caracteres de %d con tag= %d\n",
			//cuenta,
			//estatus.MPI_SOURCE,
			//estatus.MPI_TAG);
	}
	MPI_Finalize();
}
```

En su caso los que trabajamos con cómputo Paralelo e Inteligencia artificial no debe tener nunca aun `printf()`.




```
for(int i=1; i<10;i++){
    MPI_Send(mensaje,sizeof(mensaje),MPI_CHAR,i,100,MPI_COMM_WORLD);
}
```

- `i` - a qué procesador se lo voy a mandar
- 100 - ¿quién eres?, 100


> El CHAR era necesario porque solían existir muchas arquitecturas diferentes. Hoy en día tenemos [[Intel]] y [[AMD]].


Llamada al sistema bloqueante: El procesador no va a pasar de aquí hasta que 

## Ejecución

```
cristianidl@cristianidl-vivobook:~/Escritorio/computo-paralelo/computo-paralelo-programas/2026-05-06$ mpicc MPI-MaestroEsclavo-1.c -o MPI-ME
cristianidl@cristianidl-vivobook:~/Escritorio/computo-paralelo/computo-paralelo-programas/2026-05-06$ mpirun -np 10 MPI-ME
Nodo esclavo 5. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
Nodo esclavo 6. Recibiendo el mensaje.
Nodo esclavo 7. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Nodo esclavo 8. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
Nodo esclavo 9. Recibiendo el mensaje.
Nodo maestro. Mandando el mensaje.

Nodo esclavo 1. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
Nodo esclavo 2. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
Nodo esclavo 3. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
Nodo esclavo 4. Recibiendo el mensaje.
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
El mensaje es: Hola mundo desde el nodo maestro


Recibidos 50 caracteres de 0 con tag= 100
Recibidos 50 caracteres de 0 con tag= 100
```

## MPI

Message Pasing Interface fueron creadas casi a la par que los [[Sockets]]. Las llamadas al sistema que repetiremos en los sockets son SEND y RECEIVE.