# Práctica 4
Va a ser como la práctica anterior pero con hilos. En vez de con procesos, con hilos.

# Hilos

Resulta que `-lpthread` es una biblioteca no estándar de C (similar a `math.h`). Eso significa que la tendrémos que descargar debido a que `gcc` no la tiene.

Para que la tome en cuenta en la compilación se usará ese `-l` significa que enlace (`l`) la librería.

> Ubunut a veces es como Windows y asume que se les olvidó agregar esa terminación, pero esto baría dependiendo de la edición.