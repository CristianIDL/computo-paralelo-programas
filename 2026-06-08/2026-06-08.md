# CUDA

Compute Unified Device Architecture.

Arquitectura Unificada de Dispositivos de Cómputo.

Como ustedes pueden ver  siempre vamos a estar en contacto con la CPU. PSI está en la versión 4.0, esta permite pasar info entre laa CPU y la GPU, todo eso lo hace CUDA.

> **CPU:** Código secuencial (Inicializaciones y lecturas de datos de entrada)

> **GPU:** Código paralelo (Modelo SIMD)

> **CPU:** Código secuencial (Muestra de resultados, Almacenamiento de Resultados)

SIMD: SImple Instruction Múltiple Data

_Esto se puede emplear en [[Redes Neuronales]], [[Aprendizaje Máquina]], [[Algoritmos Genéticos]]. Todo para que se puedan obtener resultados más rápidamente_

Aquí en la GPU no hay que usar pantallas o teclados, solo procesamiento puro. Esto para que sea más óptimo.

## CUDA

Un programa [[CUDA]] invoca funciones paralelas llamadas [[kernels]]
- Un Kernel = función
- Un kernel se ejecuta en paralelo a través de la [[GPU]]

> Mil hilos se ejecutan en mil `cores` y cada core `contiene` un kernel

Los programas CUDA híbridos se ejeccutn en CPU y GPU. Se conectan a través de una conexión

```
#include <stdio.h>
#include <cuda_runtime.h>

__global__ void HolaDesdeGPU()
{
    printf("Hola Mundo desde GPU (device o dispositivo)!\n");
}

int main(void)
{
    printf("Hola Mundo desde CPU (host)!\n");

    HolaDesdeGPU<<<1,1000>>>();

    cudaDeviceSynchronize();

    return 0;
}
```

> "Yo quiero que siempre me salgan las cosas, y siempre pasa algo."

_Los archivos tienen terminación `.cu`_

Se compila con `nvcc CUDA-HolaMundo-1.cu -o CUDA-HolaMundo-1`

_El profesor mostró su código con 2000 CUDAcores. Su 2460 tiene 2500 cores. La arquitectura obliga a ejecutar de 1024 en 1024. El máximo son 1024._

> Les dije que podían hacerlo en el servidor de aquí de ESCOM. _(Lo visto en [[CP 2026-04-29]])_

Para sincronizar la CPU con la GPU se usa ``

Internamente usa semáforos, la teoría de la memoria compartida e hilos.

## Metiéndome a CUDA de la escuela

El código de CUDA-HolaMundo-1.cu lo escribí con `nano` una vez me metí al servidor, luego lo ejeccuté y funcionó correctamente.

(El código que le metí tenía solo <<<1,10>>>, que es lo que se ve en esta ejecución. Luego lo dejé en 1000.)


```
# g6bv1cuda23_2@ESCOM-CUDA-SERVER: ~

g6bv1cuda23_2@ESCOM-CUDA-SERVER:~$ nvcc CUDA-HolaMundo-1.cu -o CUDA-HolaMundo-1
g6bv1cuda23_2@ESCOM-CUDA-SERVER:~$ ls
CUDA-HolaMundo-1     Equipo6-Practica5  HolaMundo.cu         MPI-HolaMundo1-JAJB.c  SumaMatrices-1.cu
CUDA-HolaMundo-1.cu  HolaMundo          MPI-HolaMundo1-JAJB  SumaMatrices-1
g6bv1cuda23_2@ESCOM-CUDA-SERVER:~$ ./CUDA-HolaMundo-1
Hola Mundo desde CPU (host)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
Hola Mundo desde GPU (device o dispositivo)!
```

# Continuando con la SumaMAtrices.cu

Código completo:

```
#include<stdio.h>
#include<cuda_runtime.h>
#include<device_launch_parameters.h>

#define Tam 3

__global__ void SumaMatriz(int *A, int *B, int *C, int tam)
{
   printf("\nRealizando trabajo en el hilo (%d, %d)",threadIdx.x,threadIdx.y);
   int n=threadIdx.x;
   int m=threadIdx.y;
   C[n+m*tam]=A[n+m*tam]+B[n+m*tam];
}

int main()
{
   int i,j;
   int A[Tam][Tam]={{0,1,2},{3,4,5},{6,7,8}};
   int B[Tam][Tam]={{0,1,2},{3,4,5},{6,7,8}};
   int C[Tam][Tam]={{0,0,0},{0,0,0},{0,0,0}};
   int *devA, *devB, *devC;
   dim3 HilosPorBloque(Tam*Tam);
   dim3 BloquesPorGrid(1);

   printf("\nInicializando datos desde la CPU\n\n");

   for(i=0;i<Tam;i++)
   {
      for(j=0;j<Tam;j++)
      {
         printf("A[%d][%d]=%d\t",i,j,A[i][j]);
      }
      printf("\n");
   }
   printf("\n");
   for(i=0;i<Tam;i++)
   {
      for(j=0;j<Tam;j++)
      {
         printf("B[%d][%d]=%d\t",i,j,B[i][j]);
      }
      printf("\n");
   }
   printf("\n");
   for(i=0;i<Tam;i++)
   {
      for(j=0;j<Tam;j++)
      {
         printf("C[%d][%d]=%d\t",i,j,C[i][j]);
      }
      printf("\n");
   }

   cudaMalloc((void**)&devA,Tam*Tam*sizeof(int));
   cudaMalloc((void**)&devB,Tam*Tam*sizeof(int));
   cudaMalloc((void**)&devC,Tam*Tam*sizeof(int));

   printf("\nCopiando datos al dispositivo\n");

   cudaMemcpy(devA,A,Tam*Tam*sizeof(int),cudaMemcpyHostToDevice);
   cudaMemcpy(devB,B,Tam*Tam*sizeof(int),cudaMemcpyHostToDevice);
   cudaMemcpy(devC,C,Tam*Tam*sizeof(int),cudaMemcpyHostToDevice);

   printf("\nIniciando procesamiento paralelo al dispositivo\n");

   SumaMatriz<<<BloquesPorGrid,HilosPorBloque>>>(devA,devB,devC,Tam);

   cudaMemcpy(C,devC,Tam*Tam*sizeof(int),cudaMemcpyDeviceToHost);

   printf("\n\nMostrando datos generados desde la CPU\n\n");

   for(i=0;i<Tam;i++)
   {
      for(j=0;j<Tam;j++)
      {
         printf("C[%d][%d]=%d\t",i,j,C[i][j]);
      }
      printf("\n");
   }

   cudaFree(devA);
   cudaFree(devB);
   cudaFree(devC);

   cudaDeviceReset();

   return 0;
}
```


`cudaMemcpy(devA, cpuA, Tam*Tam)`

Lo que está en la cpu a lo que apunta acá (Host es la CPU, device es la GPU). Con esto apenas está en la memoria global

- Hilo
- Conjunto de Hilo es Bloque
- Conjunto de Bloque es Malla

> "Acuérdence que Kernel es Función."

> En este caso para su proyecto solo tienen que pasarle `Tam`. No tienen que pasarle toda la información, lo que sí es que deben almacenarlo en algún lugar, por lo que tengan que definir al menos una matriz.

Tengo que regresar los datos al kernel

`cudaMemcpy(cpuC,devC,Tam*Tam*sizeof(int), cudaMemcpyDeviceToHost)`

Al ir finalizando el código:

- Muestra todo en pantalla

- Libera todo los mallocs de cuda

- Resetea el dispositivo

_Johnny preguntó si era obligatorio resetear siempre, el profe dijo que depende de la aplicación, pero que si queda reservado para tí, esos núcleos quedarán ocupados._

> `devA` y `devB` son memoria compartida, por lo que no me importa mucho si sse alteran. Pero les paso a las 3 el tamaño por cualquiere cosa

El profe definió un bloque de 3x3. DEntro de cada hilo hay un núcleo cuda y dentro de cada uno se ejecuta un kernel.

Lo que hacemos es que cada núcleo ejecute una coordenada de la matríz, eso es lo que tiene sentido para dividir el trabajo.

> **Para poder decirle qué operación se tiene que hacer debo saber sus coordenadas. Eso lo sabemos con `threadIdx.x, threadIdy.y`**

El código toma cada coordenada de los núcleos, esto siempre será diferente porque cada uno es distinto.

# Avisos parroquiales

Este miércoles el profesor no va a dar clase porque tiene revisión de TT. Nos

Martes y miércoles no hay clase porque es la EXPOESCOM, el jueves de esa semana hay otro partidom entoncces en un buen tiempo no nos veremos.